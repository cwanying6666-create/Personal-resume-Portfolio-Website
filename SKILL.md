---
name: personal-portfolio-website
description: 当用户需要创建、重建、定制或更新设计师个人简历与作品集网站时使用本 skill，尤其适用于产品设计师、UX 设计师、UI 设计师和交互设计师岗位。它包含分阶段内容采集、项目/PDF 资料要求、简历模块、联系方式字段，以及现有个人网站的完整视觉和布局规范。
---

# 个人简历作品集网站

当用户想生成、重建、定制或整理个人简历和作品集网站时，使用本 skill。

本 skill 包含两个必须参考的文件：

- `references/content-prompt.md`：内容采集流程、用户提问方式、示例占位符和必填信息。
- `references/design.md`：视觉风格、页面布局、组件规范、响应式规则和资源规范。

## 核心规则

1. 内容和样式必须分离。
2. 内容问题按照 `references/content-prompt.md` 采集。
3. 视觉、布局、字体、色彩、组件、动效和响应式规则全部按照 `references/design.md` 执行。
4. 不要询问用户视觉风格、色彩偏好、布局偏好或是否需要微信图标。
5. 微信图标默认使用 `/wx.svg`。
6. 项目封面必须要求 16:9，推荐尺寸为 `1920x1080`。
7. 每个项目都需要用户提供 PDF，用于项目详情页生成或展示。
8. 不要编造关键履历事实。可以润色和结构化用户提供的信息，但必须保持真实。
9. 首屏必须是可用的网站内容，不要做成营销落地页。

## 工作流程

### 1. 内容采集

当用户没有提供完整内容时，读取 `references/content-prompt.md`，并按阶段采集信息：

1. 个人身份信息
2. 网站用途和目标受众
3. 项目信息、16:9 封面图和项目 PDF
4. 工作经历
5. 教育经历
6. 专业技能和软件技能
7. 联系方式
8. 可选的其他内容：其他设计作品、设计思考和推荐语

以对话方式逐步询问，除非用户明确要求完整表单，否则不要一次性抛出过长问卷。

### 2. 网站生成

当内容足够完整时：

1. 读取 `references/design.md`。
2. 按以下顺序生成单页作品集网站：
   - 固定顶部导航
   - Resume / 个人介绍
   - Project
   - Ability
   - Experience
   - Others Project
   - Contact
3. 当用户提供项目 PDF 时，支持生成或展示项目详情页。
4. 优先使用 `public/` 中的本地资源。
5. 项目卡片封面和项目详情页顶部封面都必须保持 16:9。
6. 项目图片使用 `object-fit: cover`，并从顶部开始裁切。

### 3. 实现偏好

修改已有网站时，优先沿用当前项目的技术栈。创建新本地项目时，如果用户没有特别指定，React + Vite + Tailwind 是默认推荐方案。

优先遵循仓库现有写法，再考虑新增抽象。改动范围应聚焦在作品集网站、内容提示词和视觉规范文档上。

### 4. 验证

生成或修改网站后：

1. 启动或复用本地开发服务。
2. 在浏览器中打开网站。
3. 检查桌面端和移动端布局。
4. 确认项目封面为 16:9。
5. 确认微信图标 `/wx.svg` 正常显示。
6. 确认文字不会溢出按钮、卡片或区块容器。

## 更新提示词文档时

如果用户要求更新提示词或视觉规则：

- 内容问题、占位符、示例和信息要求更新到 `content-prompt.md`。
- 风格、布局、组件、图片、响应式或交互规则更新到 `design.md`。
- 如果本 skill 已安装，且改动会影响后续 skill 行为，也要同步更新 `references/` 下的对应文件。
